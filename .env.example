# Optional fallback when NOT running as a DSH plugin.
# Prefer: DSH Settings → Mind Map → pick a model (no key here).
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
PORT=17890
BIND=127.0.0.1
